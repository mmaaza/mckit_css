to build the package:
    type: npm run build

in case Node is giving error,
(Node Sass could not find a binding for your current environment: Windows 64-bit with Node.js 20.x

Found bindings for the following environments:
  - Windows 64-bit with Node.js 18.x)

type,
npm rebuild node-sass